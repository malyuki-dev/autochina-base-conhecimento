{
  "name": "autochina-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start:back": "npm run start:dev --workspace=autochina-backend",
    "start:front": "npm run start --workspace=autochina-frontend",
    "install:all": "npm install && npm install --workspace=autochina-backend && npm install --workspace=autochina-frontend"
  }
}